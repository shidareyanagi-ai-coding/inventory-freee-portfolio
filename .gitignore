# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Local databases
*.db
*.sqlite
*.sqlite3

# A-5: 証憑の元画像（サーバ側保存。DBにはパスのみ。リポジトリには入れない）
voucher_store/

# Environment variables
.env
.env.local

# Node / tooling
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor / local tooling
.DS_Store
Thumbs.db
.vscode/
.idea/
# Claude Code のローカル設定（権限 settings.local.json・プレビュー launch.json 等。push 不要）
# 注: gitignore は行末インラインコメント非対応。パターンとコメントは必ず別行にする。
.claude/

# Generated handoff bundles
design_handoff_package/
